Q2: Write a program to input two numbers and display their sum, difference, product, and quotient.

/*
Sample Test Cases:
Input 1:
10 2
Output 1:
Sum=12, Diff=8, Product=20, Quotient=5

Input 2:
7 3
Output 2:
Sum=10, Diff=4, Product=21, Quotient=2

*/
#include <stdio.h>

int main() 
{
    float num1, num2;
    
    printf("Input number 1 : ");
    scanf("%f", &num1);

    printf("Input number 2: ");
    scanf("%f", &num2);

    float sum = num1 + num2;
    float diff = num1 - num2;
    float mul = num1 * num2;
    float div = num1 / num2;  
    
    printf("Sum is %.3f, difference is %.3f, product is %.3f and quotient is %.3f\n", sum, diff, mul, div);

    return 0;
}
